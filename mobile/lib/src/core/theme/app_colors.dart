import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const ink = Color(0xFF0F172A); // Luxury Slate Dark Navy text (var(--ink))
  static const muted = Color(0xFF64748B); // Cool Muted Slate (var(--muted))
  static const surface = Color(0xFFF8FAFC); // Soft Off-white Canvas background (var(--wash))
  static const paper = Color(0xFFFFFFFF); // Pure White cards (var(--paper))
  static const border = Color(0xFFE2E8F0); // Soft light slate borders
  static const gold = Color(0xFFD97706); // Golden Amber Accent (var(--primary))
  static const orange = Color(0xFFB45309); // Deep Gold (var(--primary-deep))
  static const emerald = Color(0xFF10B981); // Emerald success accent (var(--accent))
  static const clay = Color(0xFFEF4444); // Clay red accent
  static const navy = Color(0xFF1E3A8A); // Royal Navy Slate
  static const sky = Color(0xFF3B82F6); // Sky Blue (var(--sky))
}
